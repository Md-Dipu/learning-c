#include <stdio.h>

int main()
{
    int num1, num2, sum;          // variable declearing here
    scanf("%d %d", &num1, &num2); // user input

    sum = num1 + num2; // processing (sum)

    printf("\nResult is %d", sum); // output
    return 0;
}
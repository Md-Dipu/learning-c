#include <stdio.h>

int main()
{
    // printf("This is a complier time error")
    // printf(its a compiler time error);

    int a = 0;
    int b = 4;
    // printf("Divided: %d", b / a);

    int num1 = 8, num2 = 2;
    printf("Div is %d", num1 % num2); // logical error

    return 0;
}
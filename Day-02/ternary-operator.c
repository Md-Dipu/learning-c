#include <stdio.h>

int main()
{
    // if (0 < 1)
    //     printf("0 is less than 1.");
    // else
    //     printf("0 is not less than 1.");

    (0 < 1) ? printf("0 is less than 1.") : printf("0 is not less than 1.");
    // cond ? if cond is true : if cond is false;

    return 0;
}